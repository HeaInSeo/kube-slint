# Docs / CI Agent

Scope: README, operating docs, progress/status consistency, and workflow-facing terminology.

Rules:

- Keep correctness testing and guardrail evaluation distinct.
- Treat `docs/project-status.yaml` as automation input and prose docs as human-oriented context.
- Escalate stale or contradictory wording instead of silently normalizing it.
