current_stage: "v1.5.2 tagged; competition submission prep"
roadmap_percent: 94
roadmap_status: "post_rc"
current_focus:
  - "v1.5.2 tagged: closed GitHub issues #2/#3 via documentation (D-018 same-kind ownerRef limitation, D-019 image pinning policy) rather than fabricated code fixes, plus README(Kor).md tone unification"
  - "v1.5.1 tagged: patch fix for the slint-gate composite action losing artifacts/outputs on gate failure, init overwrite guard, kubectl discovery error surfacing, English-unified comments/diagnostics on the public surface"
  - "v1.5.0 tagged: SLI Gate Onboarding UX roadmap (Sprint 1-6), dataplane-service static analyzer, custom Semgrep guardrails"
  - "Post-RC hardening sprint (v1.4.0) complete: gate reliability/regression correctness, secret containment, RBAC scope reduction, fetcher metric normalization"
  - "pkg/gate (formerly internal/gate) provides tested, externally-importable gate evaluation logic"
  - "Quality roadmap Priority 0 implemented: ServiceURLFormat default-deny validator, kube-system namespace rejection, dangerously-named opt-ins, summary/policy bad-fixture executable tests"
  - "dataplane-service static analyzer shipped in v1.5.0: slint-gate analyze-dataplane, pkg/report, pkg/dataplane; two checks later removed as kube-linter duplicates"
  - "SLI Gate Onboarding UX roadmap complete (Sprint 1-6), shipped in v1.5.0: init --profile, promote_to_fail/--exit-on naming (dual-support with deprecated fail_on/--fail-on), inspect, recommend-policy (--strictness, --profile-file), baseline approve/diff/merge, ci github-actions, quickstart"
  - "kubebuilder-operator profile expanded 6->9 real SLIs (BaselineV3Specs already defined these); no fabricated second built-in profile added"
  - "Custom Semgrep guardrails implemented and blocking in CI, shipped in v1.5.0: 6 rules from security-model.md's previously-unimplemented Static Guardrail Plan, real codebase verified compliant"
current_focus_deferred:
  - "baseline merge review-existing/force-replace modes (only append-new-only implemented)"
  - "True interactive wizard (stdin-prompted); quickstart shipped as the non-interactive alternative"
  - "MCP read-only exposure of analyze-dataplane / IDE integration (separate track, paused)"
  - "pkg/policy and pkg/summary public package cleanup"
  - "F4: quoted label parser improvement"
next_milestone: "Competition submission prep: final regression pass, submission checklist review before the 2026-07-17 deadline (corrected from an earlier mistaken 2026-08-15 assumption)"
active_hardening_sprint: "v1.5.2-released"
capabilities:
  identity_aligned: "done"
  decision_log_added: "done"
  regression_gate_spec: "done"
  github_actions_visibility: "done"
  hello_operator_validation: "done"
  go_cli_gate: "done"
  readme_rewrite: "done"
  onboarding_ux_cli: "done"
  semgrep_guardrails: "done"
