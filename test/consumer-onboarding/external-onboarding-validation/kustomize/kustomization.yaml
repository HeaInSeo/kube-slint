apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: my-operator-system

resources:
# Pinning to the RC tag to ensure reproducible builds
- github.com/HeaInSeo/kube-slint//config/samples/prometheus?ref=v1.0.0-rc.1

patches:
- path: patch-monitor.yaml
