apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Observability stack resources live in this repo.
# TODO: Replace the placeholder resources below with your real stack manifests.

namespace: kube-slint-observability

resources:
- namespace.yaml
- placeholders/README-configmap.yaml

# Patches are optional. Uncomment and tailor as needed.
# patches:
# - path: patches/override-values.yaml
#   target:
#     kind: Deployment
#     name: <target-name>
# - path: patches/extra-config.yaml
#   target:
#     kind: ConfigMap
#     name: <target-name>
