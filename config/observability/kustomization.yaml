apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization


resources:
# Remote resource (pin ref for reproducibility):
# - github.com/<ORG>/<OBS_REPO>//kustomize/overlays/default?ref=<PINNED_REF>
#   # <PINNED_REF> should be a tag or commit SHA (commit SHA recommended).

# Optional patches (uncomment as needed):
# patches:
# - path: patches/override.yaml
#   target:
#     kind: Deployment
#     name: <target-name>
